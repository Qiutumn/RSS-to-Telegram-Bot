TOKEN=
MANAGER=
# Private instance: only the manager and explicitly authorized users/chats.
MULTIUSER=0
# For Telegram access through a proxy on the Mac, if needed:
# T_PROXY=socks5://host.docker.internal:10808
# For feeds served by the existing RSSHub on OrbStack, use its reachable URL.
